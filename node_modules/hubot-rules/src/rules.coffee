# Description:
#   Make sure that hubot knows the rules.
#
# Commands:
#   hubot the rules - Make sure hubot still knows the rules.
#
# Notes:
#   DON'T DELETE THIS SCRIPT! ALL ROBAWTS MUST KNOW THE RULES

rules = [
  "1. Keep things Safe for Work in all channels, except for NSFW channel",
  "2. Please don't SPAM",
  "3. Check out nubot-commands channel to see list of commands for nubot."
  ]

otherRules = [
  "Don't bomb any channels",
  "Brothers are brothers",
  "Contact Rhino for support or any bugs"
  ]

module.exports = (robot) ->
  robot.respond /(what are )?the (three |3 )?(rules|laws)/i, (msg) ->
    text = msg.message.text
    if text.match(/apple/i) or text.match(/dev/i)
      msg.send otherRules.join('\n')
    else
      msg.send rules.join('\n')
